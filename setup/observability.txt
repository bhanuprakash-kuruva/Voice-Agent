Great — since everything is working now, I’ll give you a **clean, reproducible command sequence starting from `loki.yaml` creation** up to **working Grafana + Loki + Tempo tracing**.
This is the **exact sequence that works with your environment**:

* Windows Browser
* WSL
* k3d Kubernetes cluster
* Grafana + VictoriaMetrics already running

I’ll keep it **minimal and correct** so you can reuse it anytime.

---

# 1. Create `loki.yaml`

In WSL:

```bash
cd ~/RAG/ollama-rag
nano loki.yaml
```

Paste:

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: loki
spec:
  replicas: 1
  selector:
    matchLabels:
      app: loki
  template:
    metadata:
      labels:
        app: loki
    spec:
      containers:
        - name: loki
          image: grafana/loki:2.9.3
          args:
            - -config.file=/etc/loki/local-config.yaml
          ports:
            - containerPort: 3100
---
apiVersion: v1
kind: Service
metadata:
  name: loki
spec:
  type: NodePort
  selector:
    app: loki
  ports:
    - name: loki-http
      port: 3100
      targetPort: 3100
      nodePort: 30100
```

Save.

---

# 2. Deploy Loki

```bash
kubectl apply -f loki.yaml
```

Verify:

```bash
kubectl get pods
```

You should see:

```text
loki-xxxxx   Running
```

---

# 3. Create `promtail.yaml`

Promtail collects Kubernetes logs and sends them to Loki.

```bash
nano promtail.yaml
```

Paste:

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: promtail-config
data:
  promtail.yaml: |
    server:
      http_listen_port: 9080

    positions:
      filename: /tmp/positions.yaml

    clients:
      - url: http://loki:3100/loki/api/v1/push

    scrape_configs:
      - job_name: kubernetes-pods
        static_configs:
          - targets:
              - localhost
            labels:
              job: kubernetes-pods
              __path__: /var/log/pods/*/*/*.log

---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: promtail
spec:
  selector:
    matchLabels:
      app: promtail
  template:
    metadata:
      labels:
        app: promtail
    spec:
      containers:
        - name: promtail
          image: grafana/promtail:2.9.3
          args:
            - -config.file=/etc/promtail/promtail.yaml
          volumeMounts:
            - name: config
              mountPath: /etc/promtail
            - name: varlog
              mountPath: /var/log
      volumes:
        - name: config
          configMap:
            name: promtail-config
        - name: varlog
          hostPath:
            path: /var/log
```

---

# 4. Deploy Promtail

```bash
kubectl apply -f promtail.yaml
```

Verify:

```bash
kubectl get pods
```

You should see:

```text
promtail-xxxxx   Running
```

---

# 5. Create Tempo Config

Tempo needs a config file.

```bash
nano tempo-config.yaml
```

Paste:

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: tempo-config
data:
  tempo.yaml: |
    server:
      http_listen_port: 3200

    distributor:
      receivers:
        otlp:
          protocols:
            grpc:
            http:

    ingester:
      trace_idle_period: 10s
      max_block_bytes: 1000000
      max_block_duration: 5m

    compactor:
      compaction:
        block_retention: 1h

    storage:
      trace:
        backend: local
        local:
          path: /tmp/tempo
```

Deploy:

```bash
kubectl apply -f tempo-config.yaml
```

---

# 6. Create `tempo.yaml`

```bash
nano tempo.yaml
```

Paste:

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tempo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tempo
  template:
    metadata:
      labels:
        app: tempo
    spec:
      containers:
        - name: tempo
          image: grafana/tempo:2.4.1
          args:
            - "-config.file=/etc/tempo/tempo.yaml"
          ports:
            - containerPort: 3200
            - containerPort: 4317
          volumeMounts:
            - name: tempo-config
              mountPath: /etc/tempo
      volumes:
        - name: tempo-config
          configMap:
            name: tempo-config
---
apiVersion: v1
kind: Service
metadata:
  name: tempo
spec:
  type: NodePort
  selector:
    app: tempo
  ports:
    - name: tempo-http
      port: 3200
      targetPort: 3200
      nodePort: 30200
    - name: tempo-otlp
      port: 4317
      targetPort: 4317
      nodePort: 30417
```

Deploy:

```bash
kubectl apply -f tempo.yaml
```

Verify:

```bash
kubectl get pods
```

You should see:

```text
tempo-xxxxx   Running
```

---

# 7. Forward Ports (Important for WSL + Windows)

Run these in **separate terminals**:

### Loki

```bash
kubectl port-forward svc/loki 30100:3100
```

### Tempo HTTP

```bash
kubectl port-forward svc/tempo 30200:3200
```

### Tempo OTLP (for tracing)

```bash
kubectl port-forward svc/tempo 30417:4317
```

---

# 8. Configure Python Tracing

Your exporter must use:

```python
OTLPSpanExporter(
    endpoint="http://localhost:30417",
    insecure=True
)
```

---

# 9. Access from Windows Browser

| Service         | URL                                              |
| --------------- | ------------------------------------------------ |
| Grafana         | [http://localhost:30300](http://localhost:30300) |
| VictoriaMetrics | [http://localhost:30750](http://localhost:30750) |
| Loki            | [http://localhost:30100](http://localhost:30100) |
| Tempo           | [http://localhost:30200](http://localhost:30200) |

Logs and traces are viewed **inside Grafana**.

---

# 10. Final Observability Architecture

You now have a **full AI observability stack**:

```
Voice RAG
   │
   ├ Metrics → VictoriaMetrics
   ├ Logs → Promtail → Loki
   └ Traces → OpenTelemetry → Tempo

Grafana
   ├ Metrics dashboards
   ├ Logs explorer
   └ Trace viewer
```

This is the **same architecture used in production ML platforms**.

---

If you'd like, I can also give you a **clean architecture diagram for your entire Voice-RAG Kubernetes system** (Ollama + RAG + Knative + Observability).
